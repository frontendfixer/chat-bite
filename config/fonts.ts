import { Open_Sans, Signika_Negative } from 'next/font/google';

export const openSans = Open_Sans({
  subsets: ['latin'],
  display: 'swap',
});

export const signikaNegative = Signika_Negative({
  subsets: ['latin'],
  display: 'swap',
});
